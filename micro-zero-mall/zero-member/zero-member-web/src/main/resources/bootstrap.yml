spring:
  application:
    name: zero-member-web
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: aliyun.whoiszxl.com:8848
      config:
        server-addr: aliyun.whoiszxl.com:8848
        file-extension: yaml