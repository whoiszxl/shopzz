spring:
  application:
    name: zero-order-web
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: aliyun.whoiszxl.com:8848
      config:
        server-addr: aliyun.whoiszxl.com:8848
        file-extension: yaml


  jpa:
    properties:
      hibernate:
        enable_lazy_load_no_trans: true