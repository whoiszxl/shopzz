server:
  port: 18888

spring:
  application:
    name: zero-authentication
  cloud:
    nacos:
      server-addr: aliyun.whoiszxl.com:8848
      discovery:
        server-addr: aliyun.whoiszxl.com:8848
      config:
        server-addr: aliyun.whoiszxl.com:8848
        file-extension: yaml


